services:
  poker:
    build: .
    container_name: poker-web
    ports:
      - "8080:8080"
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/api/game/health" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
